import styled from "styled-components/macro";

export const SectionContainer = styled.article`
  width: 100%;
  padding: 2rem 0px;
  border-bottom: 1px solid var(--color-golden-dark);

  :first-of-type {
    margin-top: 100px;
  }
`;
